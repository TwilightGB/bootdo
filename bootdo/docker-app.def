#!/bin/bash

APP=( "web" )
web=(
     "nohup  java  -jar  bootdo-2.0.0.jar &" # start command
     "APP_NAME:bootdo"       # process key
     "echo OK"    # liveness check command
     "OK"         # liveness successful code
     "echo OK"    # readiness check command
     "OK"         # readiness successful code
    )
